composer global remove laravel/installer
composer global require laravel/installer

versión actual de laravel Installer-> 5.16 01-07-2025
Se esta desarrollando con Iconos->https://heroicons.com/

- laravel new example-app

- composer require --dev laravel-lang/common -> para traducir a español
    -> php artisan lang:add es 
    -> modificar en config/app.php -> 'locale' => 'es',

- composer require spatie/laravel-permission

- npm install sweetalert2

- Configurar la Base de Datos.
    -> php artisan migrate

- Al clonar el proyecto->composer install

- Comandos Artisan:
    php artisan config:clear
    php artisan view:clear  
    php artisan migrate:fresh --seed


Git
git status -> muestra el estado del proyecto.
git add testingApp vendor -> agrega la carpeta a seguimiento
git rm -r --cached vendor/ ->remover carpeta o archivo.
git push origin master -> subir lo cambios al repositorio.
git push


